[project]
name = "action-test"
version = "0.1.0"
description = "PR AI Review Action Test Project"
requires-python = ">=3.9"
dependencies = [
    "pytest>=8.4.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
